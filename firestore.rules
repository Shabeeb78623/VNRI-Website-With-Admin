
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 🚨 PRODUCTION RULE SET 🚨
      // Since this app uses a hardcoded Admin login (without Firebase Auth),
      // we must set rules to "public" to allow the admin to save data.
      // Copy and Paste this ENTIRE block into Firebase Console > Firestore > Rules.
      
      allow read, write: if true;
    }
  }
}
