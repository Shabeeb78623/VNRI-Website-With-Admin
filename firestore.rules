rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow public read access to all documents
      // Allow public write access to all documents (Note: For production, you should secure this with Authentication)
      allow read, write: if true;
    }
  }
}